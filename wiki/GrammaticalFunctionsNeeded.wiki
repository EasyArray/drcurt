#summary These are some functions that the Discourse team needs in regards to parsing sentences

  * A function to parse yes or no statements into a logical true or false
  * A function to parse an initial listing of symptoms into a Prolog list
  * A function to ask user if they are experiencing X condition. I.E. "Do you have a cough?" Grammar should change depending on condition.